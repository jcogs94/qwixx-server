import { ConnectionHandler } from "@services/socket/connectionHandler"

export const gameHandlers = (_: ConnectionHandler) => {
    // socket.on('individualNodeModules', async (data: SocketProductPayload) => {
    //     socketLogger.verbose(`'individualNodeModules' event received`)
    //     await handleIndividualNodeModulesRequest(socket, data)
    // })
}

